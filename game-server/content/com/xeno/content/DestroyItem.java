package com.xeno.content;

import com.xeno.entity.item.Item;
import com.xeno.entity.player.Player;

public class DestroyItem {

	public DestroyItem() {
		
	}
	
	public static void displayInterface(Player p, int item) {
		/*Item[] items = {new Item(item, 1)};
		//p.getActionSender().sendItems(94, 0, 93, items);
		p.getActionSender().modifyText("Are you sure you want to destroy this item?", 94, 3); // Title
		//p.getActionSender().modifyText("Yes", 94, 4); // Yes
		//p.getActionSender().modifyText("No", 94, 5); // No
		p.getActionSender().modifyText("", 94, 10); // Line 1
		p.getActionSender().modifyText("If you wish to get another Fire cape, you must", 94, 11); // Line 2
		p.getActionSender().modifyText("complete the Fight cave minigame again.", 94, 12); // Line 3
		p.getActionSender().modifyText("Fire Cape", 94, 13); // Item name
		p.getActionSender().sendChatboxInterface(94);*/
	}
}
